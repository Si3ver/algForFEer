// 调整数组的顺序，使奇数在前偶数在后

function reOrderArray(arr)
{
    if(!(arr instanceof Array)){
        return false;
    }
    var lIdx = 0, rIdx = arr.length-1;
    while(lIdx < rIdx){
        // if 
    }
    
}

console.log(reOrderArray([213,324,43,574,865,345,234,124,7245,845,4578,325]));
